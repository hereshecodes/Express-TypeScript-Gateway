{
  "name": "legacy-bridge-ts",
  "version": "1.0.0",
  "description": "TypeScript API gateway that wraps a legacy service with metrics, caching, retries, and a circuit breaker.",
  "type": "module",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.19.2",
    "lru-cache": "^11.0.1",
    "pino": "^9.4.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
